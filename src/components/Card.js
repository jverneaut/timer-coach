import React from 'react';

const Card = () => (
  <div className="card">
    <div className="card__content">
      <strong>Drink water</strong> <em>every 2 hours</em> from 9am to 5pm.
    </div>
    <button className="card__button">
      Remove
    </button>
  </div>
);

export default Card;
